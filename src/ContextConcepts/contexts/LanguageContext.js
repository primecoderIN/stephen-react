import React from "react";

export default React.createContext("Hindi");
//Any valid javascript value can be passed as default value
//Default values can not be changed. They will remain same all time.
